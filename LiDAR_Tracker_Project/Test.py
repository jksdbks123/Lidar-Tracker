from multiprocessing import Process
import time

def thread(thread_ind,process_ind):
    while True:
        print('Thread:'{ind})


def p1():
    
    print('')




if __name__ == "__main__":

        p_1 = Process(target = self.mot_tracking, args = [self.Td_maps_queue,])
        p_2  = Process(target = self.reading_frames, args = [self.Td_maps_queue,])